name: Предпросмотр на завтра (вечером)

# Каждый день в 15:00 UTC = 18:00 по Москве.
# На одном сборе ЗАВТРАШНИХ событий: (1) список темы владельцу на выбор;
# (2) черновик «Разбора дня» на завтра владельцу на согласование.
# Если завтра суббота - присылает черновик карточки персоны недели.
on:
  schedule:
    - cron: "0 15 * * *"
  workflow_dispatch:

permissions:
  contents: write   # сохраняем список предпросмотра и черновик персоны в репозиторий

jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Установить зависимости
        run: pip install -r requirements.txt

      - name: Собрать список и прислать владельцу
        env:
          LLM_PROVIDER: yandex
          YANDEX_API_KEY: ${{ secrets.YANDEX_API_KEY }}
          YANDEX_FOLDER_ID: ${{ secrets.YANDEX_FOLDER_ID }}
          YANDEX_MODEL: yandexgpt
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_OWNER_CHAT_ID: ${{ secrets.TELEGRAM_OWNER_CHAT_ID }}
        run: python agents/publisher.py --preview --send

      - name: Черновик «Разбора дня» на завтра владельцу на согласование
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          FEATURE_PROVIDER: openai
          OPENAI_MODEL: gpt-4o-search-preview
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_OWNER_CHAT_ID: ${{ secrets.TELEGRAM_OWNER_CHAT_ID }}
        run: python agents/agent5_feature.py --preview --send

      - name: Сохранить предпросмотр и черновик персоны
        run: |
          git config user.name "nkg-bot"
          git config user.email "nkg-bot@users.noreply.github.com"
          for f in data/preview.json data/feature_preview.json data/person_choice.json data/person_tally.json; do
            [ -f "$f" ] && git add "$f"
          done
          [ -d data/archive ] && git add data/archive
          if git diff --cached --quiet; then
            echo "Изменений нет."
          else
            git commit -m "Авто: предпросмотр на завтра / черновик персоны"
            git push
          fi
